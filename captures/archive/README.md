# Capture Archive

Historical learned batches belong here.

Current subfolders:
- [legacy](C:/dev/GitHub/visulive/captures/archive/legacy) for pre-wall-clock `1969-...` captures
- `exploratory` for prior learned batches that should not pollute the active inbox

The analyzer and watcher ignore this archive by default when reading the live inbox.
